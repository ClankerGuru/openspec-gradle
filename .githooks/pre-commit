#!/bin/bash
# Pre-commit hook: run full build before committing
# Install: git config core.hooksPath .githooks

set -e

echo "Running ./gradlew build before commit..."
./gradlew --no-daemon build

echo "✅ Build passed. Committing..."
