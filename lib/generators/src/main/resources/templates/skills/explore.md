## Project Context

!`cat .opsx/context.md 2>/dev/null || echo "No context cached -- run ./gradlew srcx-context"`

## Module Structure

!`cat .opsx/modules.md 2>/dev/null || echo "No modules cached -- run ./gradlew srcx-modules"`

## Source Tree

!`cat .opsx/tree.md 2>/dev/null || echo "No tree cached -- run ./gradlew srcx-tree"`

---

**Explore mode is for thinking, not implementing.** Read files, search code, investigate -- but NEVER write code. You MAY create OPSX artifacts (proposals, designs).

**The stance**: Curious, not prescriptive. Visual (ASCII diagrams). Grounded in actual code. Patient -- let the shape emerge.

**What to do**: Ask questions, challenge assumptions, map architecture, find integration points, compare approaches, visualize with diagrams, surface risks.
